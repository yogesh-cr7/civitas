# Core (required)
numpy>=1.24

# Optional: only needed with `civitas index --embedder sentence-transformers`
# sentence-transformers>=2.2

# Optional: only needed with `civitas query --llm`
# anthropic>=0.30
# python-dotenv>=1.0

# Dev
pytest>=7.0
